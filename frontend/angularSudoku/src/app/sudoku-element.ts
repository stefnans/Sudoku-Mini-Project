export class SudokuElement {
    elem: number
    x: number
    y: number

    constructor(elem: number, x: number, y: number) {
        this.elem = elem;
        this.x = x;
        this.y = y;
    }
}
